
import * as React from 'react';
import { View, Text } from 'react-native';
import { AppProps } from '../common/props';

export function Home (props: AppProps) {
    return (
      <View>
         <Text>Home</Text>
      </View>
    );
}
