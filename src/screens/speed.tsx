import * as React from 'react';
import { View, Text } from 'react-native';
import { AppProps } from '../common/props';

export function Speed (props: AppProps) {
    return (
      <View>
         <Text>Speed</Text>
      </View>
    );
}
